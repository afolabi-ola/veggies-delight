export const metadata = {
  title: 'Shop Products',
  description:
    'Browse our fresh farm products and get them delivered in real-time.',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
